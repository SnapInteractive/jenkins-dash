'use strict';


// Declare app level module which depends on filters, and services
angular.module('jenkins', [
	'jenkins.filters',
	'jenkins.services',
	'jenkins.directives',
	'jenkins.controllers'
]);
