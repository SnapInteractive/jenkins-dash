'use strict';


// Declare app level module which depends on filters, and services
angular.module('jenkins', [
	'ui.bootstrap',
	'jenkins.filters',
	'jenkins.services',
	'jenkins.directives',
	'jenkins.controllers'
]);
